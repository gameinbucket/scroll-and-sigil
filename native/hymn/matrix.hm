import hymn:math

static temp = [16]float32
static copied = [16]float32

def identity(matrix [16]float32)
    matrix[0] = 1.0

def perspective(matrix [16]float32, left float32, right float32, bottom float32, top float32, near float32, far float32)
    matrix[0] = (2.0 * near) / (right - left)
